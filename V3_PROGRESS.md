# v3 重设计进度与关键决策

参考站点实测数据见 `/home/ubuntu/research/leaderboard_study.md`，设计规格见 `DESIGN_V3.md`，
数据回填记录见 `BACKFILL_V3.md`。

## 已完成

### 数据密度（阶段 3）
| 字段 | 改版前 | 改版后 |
| --- | --- | --- |
| 有价格 | 37 | 96 |
| 有上下文 token 数 | 28 | 99 |
| 有发布日期 | 0 | 100 |
| 出处缺失 | — | 0 |

散点图可画点数 28 → 58。

关键处置：
- Tencent Hy3 价格记为 0.000（会赢得所有比价）→ 清空，人民币定价留在备注，**不自行换算汇率**
- Inkling-Small 价格 0.58/1.44 与官方不符 → 修正
- fable-5（厂商 Other）与 claude-fable-5 是同一模型 → 归并
- muse-spark 与 muse-spark-1-1 是**不同版本**（262K vs 1M，发布日期不同）→ 不归并，清掉误填的价格
- `formatContextWindow` 改了三版：前两版试图「猜」进制，只要两个候选商都合理，答案就取决于试的顺序。最终判据：**2 的幂用 1024，其余用 1000**

### 令牌层（阶段 4）
- 画布抬亮 `#0c0f12` → `#15171a`，卡片才有层次
- 七色分类色板，**暗亮各自独立求解**（同色相不同明度），暗底 ~6:1 / 亮底 ~5:1
  - 曾试图求一个值通吃两个主题，结果七色全部收敛到 4.21:1 且发灰 —— 伪需求
- 旧 `frost-*` 类名保留为别名指向新令牌，124 处旧用法不失效
- 字号下限 12px → 14px（参考站表格全是 14–16px，表头靠字重而非小型大写）

### 骨架与首页（阶段 5）
- 常驻双层顶栏取代浮岛，切页不需先开目录页
- 首页改为：冠军条（6 张）→ 总榜（表格/散点/条形三视图）→ 方法学后移
- 新增 `meta.champions`：顺序选取并排除已占用模型，保证六张卡给出**六个不同答案**
  - 第一版去重后只剩 5 张（openWeight 被丢弃）—— 丢弃比重复更糟，改为选次优
- 散点图加**帕累托前沿**：连线 + 常驻标签，其余点悬停才显示。前沿是这张图唯一有决策价值的信息

### 页面改造（阶段 6）
8 个页面并行改造完成。编译器抓到 4 处问题，其中两处值得记：
- Admin.tsx 给陈旧证据表加了 ProviderDot，但**那个数据结构没有 provider 字段** —— 按规则做但数据不支持
- Decide.tsx 引用了 `MiniBars`，那是我在规则文档里写进"可用组件"清单但**实际没实现**的 —— 我的文档错了

### 矩阵响应归一化
828 KB → 294 KB（-64%）。原估计能降一个数量级，实际只到 1/3，因为 `sourceUrl`/`sourceName`
是**每条记录独有的**（占 19%），无法去重。已补 3 项等价性测试。

## 待办
- 移动端与桌面挂件适配新体系（Mobile.tsx 782 行 / Desktop.tsx 294 行，未改造）
- 双语 × 双主题 × 三端验收
- 每日快照表已建（`rankSnapshots`）但**尚未写入**，趋势/排名变动功能仍无数据

## 反复踩到的坑
截图抓在 hydration 之前会显示 0 或骨架屏，已误判三次（矩阵页两次、首页一次）。
判断方法：先 curl 接口确认返回正常且够快，再重抓截图，**不要**直接改数据层。

## 验收阶段（阶段 8）发现并修复

验收工具：`client/public/qa-preset.html?lang=&theme=&to=` —— 上次用 localStorage
驱动双主题截图遇到并发写入竞态（两张都是亮色），这个页面把两个键一次写完再硬跳转，
每次截图自成一体。

| 问题 | 根因 | 修法 |
| --- | --- | --- |
| 指标库筛选芯片压到右侧边注 | `FilterSelect` 内层芯片行缺 `flex-wrap`，7 个评分机制芯片成了不可断行的整体 | 内层加 `flex-wrap min-w-0` |
| 模型库综合分数字被条压住、溢出到证据列 | 容器 `w-24`(96px) 装不下 ScoreBar 的 42px 数字 + 80px 最小轨道 + 间距 | 放宽到 `w-[150px]` |
| 首页副标题显示「— 个模型、— 项评测」 | 加载态占位用破折号，读起来像故障 | 改为骨架条 |

已验收通过：英文暗色（默认）× 4 页、中文亮色 × 5 页、移动端 5 tab、桌面挂件。
三端各自保持独立范式，厂商配色在三端一致（Anthropic 蓝 / OpenAI 青 / Moonshot 珊瑚 /
Qwen 绿 / DeepSeek 紫）。

## 指标正文双语（阶段 8 后段）

英文界面下指标详情的「解读警示 / 落地场景 / 备注」三段正文仍是中文。这不是 UI 文案，
而是 95 条指标各自的**内容数据**（283 段），所以字典和 `noServerCopy.test.ts` 都扫不到——
守卫只看源码字符串，看不到数据库。

做法：
1. 迁移 `0003_serious_butterfly.sql` 加 `scenarioMappingEn` / `interpretationCaveatEn` / `notesEn`
2. 并行翻译 95 条（28184 字），审计脚本 `scripts/audit_prose.py` 检查 slug 匹配、残留 CJK、
   markdown 围栏、全角标点、空值一致性 —— 95/95 全清
3. `client/src/i18n/prose.ts` 的 `useProse()` 在渲染层挑语言变体；**服务端两版都返回**，
   这样响应仍与语言无关、仍可缓存，不重蹈"服务端下发中文"的老路

## `?lang=` / `?theme=` 查询参数

localStorage 驱动的验收页在截图工具的浏览器上下文里不可靠（写入后需一次导航才生效，
截图抓在跳转前）。改为让 `readStoredLang()` 与 `ThemeProvider` 优先读 URL 参数。
副产品是真实用户也能分享指定语言/主题的链接。验收改用 `/models?lang=en&theme=light`。

## 能力覆盖列：三次尝试

| 方案 | 结果 |
| --- | --- |
| 4 个文字芯片 + flex-wrap | 英文标签长，竖排成一列，行高 >100px |
| 2 个芯片 + 188px 定宽 + truncate | 截断成 `Compos...` 读不出；表格变宽把右侧边注挤到按钮上 |
| **每个能力域一个厂商色圆点 + 总数** | 64px 定宽，中英文都不受影响，完整列表挂 title |

教训：表格里的分类信息，语言无关的图形编码比文字标签稳。参考站也是这么做的。
