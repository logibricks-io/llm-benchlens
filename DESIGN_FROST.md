# BenchLens 视觉体系实施规范（霜色 / FROST）

用户已确认的三项决定：**宋体正文**、**「尺」作为全站形式母题**、**总览页改滚动叙事**，
外加我建议并获准的**夜霜深色变体**。本文件是实施期的唯一口径，改任何页面前先读这里。

## 一、双层色规则（不可违反）

实测 WCAG 对比度（底色 `#e8eff5`）：原色板七色**全部低于 4.5:1**，霜青 `#7086a0` 最高仅 3.23:1。
因此：

- **第一层 · 原色板**（`--frost-*`）：只做**填充、图形、刻度线**。**永不承担文字。**
- **第二层 · 墨霜阶**（`--ink-900..400`）：承担**全部文字与描边**。
  - `ink-900` 13.0:1 大数字 / `ink-800` 9.6:1 标题 / `ink-700` 7.3:1 强调
  - `ink-600` 5.8:1 正文 / `ink-500` 4.5:1 注解 / `ink-400` **低于 AA，只能做发丝线与图形**
- **信号色**须用压深版（`--sig-good/caution/danger/neutral`，4.2–5.2:1）才可承担文字。
- 信号色总面积**任意一屏不超过 5%**。绝不平均铺开五色。

## 二、形式母题：尺（`client/src/components/Ruler.tsx`）

产品论点是「不同评测的尺子长度不同」，所以形式直接就是尺：

- 尺的**物理长度 ∝ 难度系数**（语料实测区间 0.61–2.03，`MAX_DIFFICULTY = 2.03`）
- 原始分 = 尺上的读数；归一化 = **投射到中性尺**的可见动作
- 三种尺度复用同一母题：
  - `Ruler` 全尺寸（详情页主图、入口区）
  - `MiniRuler` 行内（矩阵单元格、能力域覆盖、体检行）
  - `ProjectionRuler` 归一化演示（总览页视口 2）

**演示数值必须调用 `normalizedScore()` 真实引擎**，不得用示意公式估算——
演示与矩阵不一致会摧毁整个论证。

## 三、布局范式

| 页面 | 范式 |
|---|---|
| 总览 | 四视口滚动叙事：论点 → 尺对比演示 → 散点+悬挂边注 → 非等权入口 |
| 矩阵 | 刊物式表格：无卡片边框、表头旋转、数字 + 短刻度替代色块热力 |
| 详情 | 主图是一把横贯全屏的大尺 |
| 外壳 | 编号索引导航（数字替代图标）、标题悬挂宽左边距、只用发丝线分区 |

全站禁止：卡片阴影、圆角盒子堆叠、等宽卡片网格、毛玻璃、图标栏。
分区手段只有三种：**发丝线、对齐、留白**。

## 四、字体

- 正文中文 `Noto Serif SC`（宋体，`--font-sans`）——这是「学术审查」与「监控告警」的气质分水岭
- 西文标题 `Instrument Serif`（`.display`）
- 数字 `JetBrains Mono`（`.tnum`，tabular-nums）
- 界面小字 `Inter`（`.ui`）——12px 以下宋体可读性下降，界面元素用无衬线

## 五、动效（只保留有解释功能的）

1. 刻度生长 `.anim-ruler` 280ms（尺进入视口时从左画出）
2. 投射线绘制 `.anim-projection` 420ms（演示归一化算法）
3. 尺度对比：同屏并置大数字与小注解，靠尺度差建立层次而非颜色
4. 全部 ≤300ms（投射线例外，它是叙事），并在 `prefers-reduced-motion` 下降级

禁止：入场淡入堆叠、装饰性视差、无意义的 hover 放大。

## 六、技术注意

- SVG 的 `fill`/`stroke` **不要用 CSS 变量或 oklch()**——解析失败会静默回退为不透明黑。
  象限底色、网格一律用绝对定位的 div。
- 不用 `oklch(from ...)` 相对色语法，浏览器支持不齐；用显式变量（`--hair-faint`、`--grain-dot`）。
- Tailwind 4 的 `@theme inline` 里注释不能出现 `*/` 之外的提前闭合。

## 七、参考

- 提案与线框：`/home/ubuntu/design/BenchLens_视觉体系提案.md`
- 色板预览：`/home/ubuntu/design/frost_palette.png`
- 布局线框：`/home/ubuntu/design/frost_wireframe.png`
- 研究笔记：`/home/ubuntu/research/design_frost.md`
- 趋势依据：Envato 2026 web design trends（anti-grid / editorial / floating minimalism）；
  Chinese Color Atlas 宋代美学色板（60/25/10/5 用量比与低对比禁忌）

---

## 八、改造进度

| 页面 | 状态 | 形态要点 |
| --- | --- | --- |
| 总览 `/` | 完成 | 四视口滚动叙事；尺母题用真实引擎演算 |
| 指标矩阵 `/matrix` | 完成 | 42° 旋转表头；数字 + 短刻度替代色块热力 |
| 指标库 `/benchmarks` | 完成 | 刊物式档案条目；每条一把真实长度的尺 |
| 模型库 `/models` | 完成 | 发丝线表格 |
| 对战台 `/compare` | 完成 | 虚线框空态 |
| 场景决策 `/decide` | 完成 | 场景清单 + 证据条 |
| 发布雷达 `/radar` | 完成 | 时间轴点线 |
| 数据运维 `/admin` | 完成 | 来源构成条形 |
| 指标详情 `/benchmarks/:slug` | 完成 | 元数据表 + 分数榜 + 版本谱系 |
| 移动端 `/m` | 待核对 | 卡片流、滑动对比、雷达 |
| 桌面挂件 `/desktop` | 待核对 | 唯一允许 `shadow-frost` 与 `hair-all` 的表面 |

移动端与桌面挂件已核对完成：移动端五 tab 卡片流全部霜色化，挂件模拟桌面改为冷灰渐层
（原为硬编码纯黑 `oklch(0.11 0.006 260)`，属旧深色时期残留，会让霜色挂件看起来像贴在夜里）。

### 已修的两个自伤缺陷

1. 散点图曾铺两块大面积象限底色（各约占 1/4 画面），违反自定的信号色 5% 面积上限。
   已改为角标标示，不铺底色。
2. `scripts/frostify.mjs` 首版对**全文**做正则替换，`\bpanel\b` 把 `Desktop.tsx` 的状态变量
   `panel` 改成了 `hair-t`，破坏代码，也污染了若干注释。已改为**仅在 `className` 字符串内**
   替换（含 `cn()` 内的字面量）。类名 codemod 绝不可看到类名以外的东西。

### 数据口径注意

`benchmarks.currentSotaScore` 是自由文本（`"42.7%"`、`"1315 Elo"`、`"12.6% (GPT-5 high)"`、
`"74% ±4%"`）。画在尺上必须先用 `parseLeadingNumber()` 取首个数值，且**只有落在 0–100 区间
才可上尺**；Elo 读数不属于这把尺，改为文字注记。早期直接 `Number()` 解析失败返回 NaN，
导致所有 SOTA 标记塌在尺的原点。

### 另外两个已修的界面缺陷

3. 移动端 tab 读的是 `window.location.search`，而 wouter 的 `location` **不含 query**，
   因此渲染期读取不具反应性：外部设置的 `?tab=...` 从不生效，系统返回手势也留在旧面板。
   已改为把 query 镜像进 state，并在 `location` 变化与 `popstate` 时重新同步。
4. PWA 安装提示常驻底部，永久压住列表最后一行。已改为延后 4s 出现、9s 后自动收起为
   右下角小徽标、并用 `sessionStorage` 保证一会话只出现一次。
