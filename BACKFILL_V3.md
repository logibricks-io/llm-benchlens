# v3 数据密度回填记录

**作者**: LogiBricks.AI Lab
**联系方式**: contact@logibricks.ai
**日期**: 2026-08-17

## 起因

用户要求参照 llm-stats 等站重做视觉。测量库内真实覆盖率后发现，比配色更根本的问题是
**表格大部分格子是空的**：353 个模型中只有 37 个有价格、28 个有上下文窗口、0 个有发布日期。
llm-stats 的表看起来密实是因为每行 11 列都有值；照搬同一套布局只会得到一张更大的空表。
因此 v3 的第一步是补数据，而非改样式。

## 结果

| 字段 | 回填前 | 回填后 | 出处缺失 |
| --- | --- | --- | --- |
| 价格（USD / 1M tokens） | 37 | 96 | 0 |
| 上下文窗口（token 整数） | 28 | 99 | 0 |
| 发布日期 | 0 | 100 | 0 |
| 商业备注（分档 / 受限说明） | — | 66 | — |

「质量 × 价格」散点图的可画点数从 28 涨到约 90。

## Schema 变更

`models` 表新增：

- `contextTokens`（int）：可排序、可上对数轴的 token 整数。原 `contextWindow` 字符串保留供显示。
- `priceSourceUrl` / `contextSourceUrl` / `releaseSourceUrl`：商业字段的出处链接。
- `commercialNote`（text）：分档定价、受限访问等必须让读者知道的前提。

新增 `rankSnapshots` 表：每模型每日一行综合分与排名，为将来的「排名变动」积累数据。
**在同一模型累积到两个不同快照日之前，界面不得声称任何趋势。**

### 为什么要加出处字段

`scores` 表一直强制每条分数带 `sourceUrl`，但价格与上下文是后加的字段，当时没有对应的出处列。
结果是库里长期存着 8 条无法追溯的价格。加字段之后这 8 条立刻暴露。

## 处置决定（三处不是简单填值）

1. **Tencent Hy3 价格记为 0.000 / 0.000 且无出处 → 清空。**
   0 不是「免费」，是「没采到」。一个错误的 0 会赢得所有比价、并在对数价格轴上跑向负无穷。
   复查后确认腾讯云以人民币计价（1 元 / 4 元每百万 token），官方未公布美元价；
   按实时汇率换算会制造假精确，因此美元价保持空缺，币种信息写入 `commercialNote`。

2. **Inkling-Small 的价格是错的。**
   库中记录 0.58 / 1.44，与官方定价不符，已按官方值更正。若不查证，它会长期占据「最便宜」榜位。

3. **97 个模型全部声称「可验证存在」，此结论经抽查后接受。**
   抽查了最不像公开产品线的名字（fable-5 / claude-mythos-5 / muse-spark / inkling /
   longcat-flash-thinking-2601），备注中含真实查证痕迹，例如 Mythos 5 标注为
   Project Glasswing 受限伙伴访问、Inkling 说明「开源权重，价格取托管商费率」。

## 并行结果不可盲信的两个实例

- 有子任务把输入的 `glm-5-1` 「订正」为 `glm-5.1`。若照抄，UPDATE 会匹配零行并**静默失败**。
  生成 SQL 时改为以输入行的 slug 为准，不采信返回字段。
- `fable-5` 与 `claude-fable-5` 返回完全相同的价格、上下文与日期，且 `fable-5` 的厂商在库中记为
  "Other"。这是同一模型的重复实体，待实体归并处理（见 todo）。

## 固化的守卫

`server/commercialProvenance.test.ts` 六项：

1. 任何价格必须有出处
2. 任何上下文窗口必须有出处
3. 任何发布日期必须有出处
4. 禁止零价
5. 价格必须落在 0.01–200 USD 的可信区间
6. 有上下文字符串就必须同时有 token 整数（否则会被散点图静默丢弃）

第 6 条在写完当即抓出 3 个漏网模型（claude-sonnet-5 / hy3 / mimo-v2-pro），已补齐。
