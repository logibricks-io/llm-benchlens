# BenchLens 设计规范 v2 — 清晰度重标定

> v1（霜色白底 + 宋体正文）被用户判为"发虚、对比度不够、字体模糊"。
> 本文档记录 v2 的实测依据与硬性约束，改任何样式前必须先读这里。

## 1. 参考页实测数据（OpenAI 发布页，getComputedStyle 实测）

来源：https://openai.com/index/introducing-gpt-5-5/

| 项 | 实测值 |
|---|---|
| body 背景 / 文字 | `rgb(255,255,255)` / `rgb(0,0,0)` → **21:1** |
| 字体 | `"OpenAI Sans", sans-serif`（**无衬线**） |
| body 字号 | **17px** |
| 正文 p | 17px / 400 / 行高 **28px**（1.65） |
| h1 | **59.19px / 500 / 行高 59.87px / 字距 -1.78px** |
| 表格 td | **14px** / 400 / 纯黑 / padding 12px |

结论：大字号才收紧字距（-1.78px），正文不收；表格 14px 不是 11px。

## 2. v1 发虚的根因（三重叠加，缺一不可）

1. 底色 `#e8eff5` 在画任何字之前就已让掉对比余量
2. 墨霜阶正文层约 5–6:1（最深仅约 8:1）
3. 12–13px 宋体竖笔不足 1 设备像素，抗锯齿把它渲染成灰雾

所以单独调任何一项都不够，必须整体重标定。

## 3. v2 色阶（数值解算，非目视）

解算脚本：`/home/ubuntu/design/calibrate.py`（二分搜索 OKLCH 明度命中目标对比度）

**暗色（默认）** 画布 `#0c0f12`：

| 令牌 | 对比度 | hex | 用途 |
|---|---|---|---|
| `--ink-950` | 17.0:1 | #eff1f3 | 大号数字 |
| `--ink-900` | 15.0:1 | #e0e3e7 | 表格数字 |
| `--ink-800` | 13.0:1 | #d0d5d9 | 标题 |
| `--ink-700` | 11.0:1 | #bfc4ca | **正文** |
| `--ink-600` | 9.0:1 | #acb2b9 | 次级正文 |
| `--ink-500` | 6.5:1 | #90979f | 注释 |
| `--ink-400` | 4.6:1 | #757d85 | 弱标签（仍达 AA） |
| `--ink-300` | 3.0:1 | #586069 | **仅图形，禁止用于文字** |

**亮色** 画布纯白 `#ffffff`（放弃霜白作正文底）：
ink-950 18:1 #151719 / ink-900 15:1 #24272b / ink-800 13:1 #2e3236 /
ink-700 11:1 #383d42（正文）/ ink-600 9:1 #444a51 / ink-500 6.5:1 #585f67 /
ink-400 4.6:1 #6e7680 / ink-300 3.0:1 #8c96a1（仅图形）

**霜色降级为强调色**，各自解算到暗底 6:1 / 白底 4.8:1，因此现在**可以承载文字**：

| 令牌 | 暗底 hex | 白底 hex | 语义 |
|---|---|---|---|
| `--acc-qing` 霜青 | #7b92ac | #5f748d | 争夺中 / 主强调 |
| `--acc-zhuqing` 竹青 | #849757 | #67793a | 良好 / 前沿 |
| `--acc-taoju` 陶橘 | #b78744 | #986924 | 注意 |
| `--acc-danzhu` 丹朱 | #d77365 | #b65549 | 危险 / 已饱和 |
| `--acc-ouhe` 藕荷 | #908e9f | #727081 | 中性 |
| `--acc-taohe` 陶褐 | #a18b88 | #836d6b | 弱化 |

## 4. 字体系统

- **正文改无衬线**：`Inter, Noto Sans SC, -apple-system, …`
- **衬线仅用于 ≥28px 的展示标题**（`.display` = Instrument Serif / Noto Serif SC）
- 数字用 `.tnum`：等宽 + `font-weight: 500` + 微负字距（400 字重的 13px 数字列最先发虚）
- body 基准 **15px / 行高 1.6**（参考页 17px 是散文页，这里是密集表格，15px 是诚实的中间值）

## 5. 硬性约束（不得违反）

1. `--ink-300` 及更弱**永不承载文字**，仅用于分隔线与图形
2. 正文字号下限 **14px**，辅助文字下限 **12px**（v1 用到 10px 是错的）
3. 表格数字最小 **13px** 且必须 `.tnum`（含 500 字重）
4. 分隔线必须可见（`--border` 约 3:1），v1 的 0.14 alpha 发丝线让结构消失，观感即是"模糊"
5. 默认主题 **dark**；`.light` 为变体
6. 主题类名挂在 `<html>` 上，切换需同时移除另一个类
